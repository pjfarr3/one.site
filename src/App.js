// src/App.js (TEMP TEST)
import React from 'react';

export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: 'system-ui, sans-serif' }}>
      <h1>App loaded ✅</h1>
      <p>If you can see this, rendering is healthy. Next, replace this test file with the full app below.</p>
      <p>Time: {new Date().toLocaleString()}</p>
    </div>
  );
}
